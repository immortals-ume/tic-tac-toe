import type {CapacitorConfig} from '@capacitor/cli';

const config: CapacitorConfig = {
    appId: 'com.immortals.ticTacToe',
    appName: 'tic-tac-toe',
    webDir: 'build'
};

export default config;
